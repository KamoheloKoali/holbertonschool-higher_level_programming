-- SQL command to list all records of the table in the database, displaying both score and name, ordered by score (top first)

-- SQL query to select all records from the table, ordering by score (top first)
SELECT score, name
FROM second_table
ORDER BY score DESC;
